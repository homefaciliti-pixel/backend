const mysql = require('mysql2/promise'); require('dotenv').config(); async function test() { const connection = await mysql.createConnection({ host: process.env.MYSQL_HOST, user: process.env.MYSQL_USER, password: process.env.MYSQL_PASSWORD, database: process.env.MYSQL_DATABASE, port: process.env.MYSQL_PORT }); const [rows] = await connection.query('SELECT id, title FROM services WHERE id IN (580, 581)'); console.log('Services:', rows); await connection.end(); } test().catch(console.error);
